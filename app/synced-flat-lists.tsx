import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import SyncedLists from '@/components/flat-lists/synced-flat-lists'

const SyncedFlatlistPage = () => {
  return (
    <SyncedLists />
  )
}

export default SyncedFlatlistPage

const styles = StyleSheet.create({})